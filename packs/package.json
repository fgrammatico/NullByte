{
  "name": "nullbyte-behavior-pack",
  "version": "0.0.24",
  "description": "NullByte — HEXCORE cybersecurity evaluation platform for Minecraft Bedrock",
  "private": true,
  "scripts": {
    "prepare-private": "node scripts/prepare-private.mjs",
    "build": "npm run prepare-private && tsc --build",
    "test": "node --test test/*.test.mjs",
    "watch": "npm run prepare-private && tsc --build --watch",
    "mcaddon": "npm run prepare-private && zip -r NullByte.mcaddon behavior_pack resource_pack && echo 'Built: packs/NullByte.mcaddon'",
    "release": "node scripts/release/publish-release.mjs",
    "release:prepare": "node scripts/release/prepare-release.mjs"
  },
  "devDependencies": {
    "@minecraft/server": "^2.7.0",
    "adm-zip": "^0.6.0",
    "typescript": "^5.4.0"
  }
}
